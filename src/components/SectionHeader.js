const SectionHeading = ({ sectionName }) => {
	return (
		<div className="sec-heading-cont">
			<h3 className="sec-heading">{sectionName}</h3>
			<div className="heading-line" />
		</div>
	);
}

export default SectionHeading;